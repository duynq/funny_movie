<% if @movies.present? %>
  <% @movies.each do |movie| %>
    <%= render partial: 'movie', locals: { movie: movie } %>
  <% end %>
  <%#= paginate @movies %>
<% else %>
  <p>There is no video is shared</p>
<% end %>